import { motion } from 'framer-motion';
import styled from 'styled-components';

const StyledAnswer = styled(motion.p)`
	width: 70%;
	padding: 2rem;
	margin: auto;
	background-color: #ffa559;
	border: 4px solid #454545;
	border-radius: 1rem;
	font-size: 1.5rem;
	color: #454545;
	cursor: pointer;
	
`;

export default StyledAnswer;